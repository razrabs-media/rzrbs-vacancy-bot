export enum FormatOfWork {
  Hybrid = "hybrid",
  OnSite = "onsite",
  Remote = "remote",
}

export enum SalaryType {
  Gross = "gross",
  Net = "net",
}

export enum EmploymentType {
  FullTime = "fulltime",
  PartTime = "parttime",
  Contract = "contract",
  Internship = "internship",
}
